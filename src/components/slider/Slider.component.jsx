
import { Box, Container, Typography } from "@mui/material";
import  "./slider.module.css";

const Slider = () => {
  return <>
  
  <Box>
    <Container>
      <Typography variant="body1" color="initial">Slider</Typography>
    </Container>
  </Box>
  </>;
};

export default Slider;
